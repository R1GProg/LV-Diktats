{
  "name": "visualizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "prebuild": "rimraf build && mkdir build",
    "build": "((browserify index.ts -p [ tsify --noImplicitAny] --s visualizer && cat visualizerEvents.js) > build/visualizer.js) && cp visualizer.css build/visualizer.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^3.0.2",
    "tsify": "^5.0.4"
  }
}
