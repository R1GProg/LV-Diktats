{
  "name": "@shared/visualizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "prebuild": "rimraf build && mkdir build",
    "build": "node browserify.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "brfs": "^2.0.2",
    "rimraf": "^3.0.2",
    "tsify": "^5.0.4",
    "uglifyify": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^18.14.6"
  }
}
